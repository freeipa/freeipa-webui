# Scenario: no-DNS

This scenario provides a single IPA server, without embedded DNS or KRA.
It uses the same IP address as the default scenario.

## Hosts

- webui:
  - Hostname: webui.ipa.test
  - IP address: 192.168.56.10
  - AD Trust
